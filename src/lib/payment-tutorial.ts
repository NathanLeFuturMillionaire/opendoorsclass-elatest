/**
 * Vidéo tutoriel de paiement affichée juste après la section tarif.
 * Placer ici l'import de l'asset dès qu'il est disponible, par exemple:
 *   import tuto from "@/assets/payment-tutorial.mp4.asset.json";
 *   export const PAYMENT_TUTORIAL_VIDEO: string | null = tuto.url;
 */
export const PAYMENT_TUTORIAL_VIDEO: string | null = null;
export const PAYMENT_TUTORIAL_POSTER: string | null = null;
